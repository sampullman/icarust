{
  "name": "icarust-web",
  "private": true,
  "type": "module",
  "description": "Browser host page for the Icarust wasm client.",
  "scripts": {
    "build": "node build.mjs",
    "build:release": "node build.mjs --release",
    "dev": "node build.mjs && vite --no-open",
    "serve": "vite --no-open",
    "preview": "vite preview --no-open"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^5.4.0"
  }
}